build:
  docker:
    web: Dockerfile

run:
  #web: gunicorn --bind :$PORT --pythonpath Web_Server/Server projeto_pgi.wsgi
  web: python Web_Server/Server/manage.py runserver $PORT

release:
  image: web
