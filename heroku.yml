build:
  docker:
    worker: Dockerfile

release:
  image: worker

run:
  #worker: gunicorn --bind :$PORT --pythonpath Web_Server/Server projeto_pgi.wsgi
  worker: python manage.py runserver 0.0.0.0:$PORT
